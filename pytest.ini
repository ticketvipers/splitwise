[pytest]
testpaths = tests
norecursedirs =
    .git
    .openclaw
    node_modules
    worktrees
    splitwise
    splitwise-app
    splitwise-backend
    **/__pycache__
addopts = --import-mode=importlib
